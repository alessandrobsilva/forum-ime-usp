<% if current_user.admin? %>
  <title><%= t(:editing_course) %></title>
  <h3><%= t(:editing_course) %></h3>
  <%= render 'form', course: @course %>
  <%= link_to t(:back), root_path, :class => 'btn btn-default btn-xs' %>
<% else %>
  <title><%= t(:forbidden) %></title>
  <h1><%= t(:forbidden) %></h1>
<% end %>
